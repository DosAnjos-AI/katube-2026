# FASE 1: PyTorch + CUDA 12.x
# Instalar PRIMEIRO para estabelecer base compativel

# PyTorch com CUDA 12.x (compativel com CUDA 12.9)
torch==2.5.1
torchaudio==2.5.1
torchvision==0.20.1

# Dependencias core do PyTorch
numpy<2.0.0
filelock>=3.9.0
typing-extensions>=4.8.0
sympy>=1.12
networkx>=3.0
jinja2>=3.1.0
fsspec>=2023.1.0

# NVIDIA CUDA libraries (sera instalado automaticamente com torch)
# Nota: PyTorch 2.5.1 vem com suporte CUDA 12.4, compativel com seu CUDA 12.9
